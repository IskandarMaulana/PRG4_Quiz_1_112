@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model List<PRG4_Quiz_1_112.Models.mahasiswa>
@{
    ViewData["Title"] = "Master Mahasiswa";
    int no = 1;
}
<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Master Mahasiswa</h5>
                    <hr />

                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="row">
                            <div class="alert alert-success alert-dismissible fade show" role="alert">
                                <i class="bi bi-check-circle me-1"></i>
                                <strong>Sukses!</strong> @TempData["SuccessMessage"]
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        </div>
                    }

                    @* <a class="btn btn-primary" asp-action"form">Tambah Mahasiswa</a> *@
                    <a  asp-action="form" class="btn btn-primary">Tambah Mahasiswa</a>


                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th scope="col">No</th>
                                <th scope="col">Nama</th>
                                <th scope="col">Program Studi</th>
                                <th scope="col">Angkatan</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*<tr>
                            <td>1</td>
                            <td>Boyolali</td>
                            <td>Roni Prasetyo</td>
                            <td>Astratech</td>
                            <td>1234-5678</td>
                            <td>2020</td>
                            <td>
                            <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                            <button class="btn btn-sm btn-primary"><i class="fas fa-edit"></i></button>
                            <button class="btn btn-sm btn-info"><i class="fas fa-info-circle"></i></button>
                            </td>
                            </tr>
                            <tr>
                            <td>2</td>
                            <td>Indonesiaku</td>
                            <td>Roni Prasetyo</td>
                            <td>Xyz Publishing</td>
                            <td>1234-3238</td>
                            <td>2019</td>
                            <td>
                            <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                            <button class="btn btn-sm btn-primary"><i class="fas fa-edit"></i></button>
                            <button class="btn btn-sm btn-info"><i class="fas fa-info-circle"></i></button>
                            </td>
                            </tr>*@

                            @foreach (var mhs in Model)
                            {
                                <tr>
                                    <td>@(no++)</td>
                                    <td>@mhs.nama</td>
                                    <td>@mhs.program_studi</td>
                                    <td>@mhs.angkatan</td>

                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    @* <script>
    $(document).ready(function () {
    $(".delete-button").click(function () {
    var bukuId = $(this).data("id");

    swal({
    title: "Hapus Buku",
    text: "Apakah Anda Yakin ingin menghapus buku ini?",
    icon: "warning",
    buttons: true,
    dangerMode: true,
    }).then((willDelete) => {
    if (willDelete) {
    $.ajax({
    url: "/Buku/Delete/" + bukuId,
    type: "POST",
    success: function (data) {
    if (data.success) {
    swal("Sukses!", data.message, "success");
    location.reload();
    } else {
    swal("Gagal", data.message, "error");
    }
    },
    error: function () {
    swal("Error", "Terjadi kesalahan saat menghapus buku", "error");
    }
    });
    }
    });
    });
    });
    </script> *@

</section>
